const CONTENT_PATH = '/buynomics_task';

export const ROUTE_PATH = {
    home: CONTENT_PATH,
    intermediaryAdd: `${CONTENT_PATH}/intermediary/add`,
    intermediaryEdit: `${CONTENT_PATH}/intermediary/:intermediaryId`,
}